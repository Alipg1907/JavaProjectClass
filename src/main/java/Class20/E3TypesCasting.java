package Class20;
/*
public class E3TypesCasting {
    public static void main(String[] args) {
        Student s1 = new Student("Aslan", "A123");
        Student s2 = new Student("Mikhail", "12345");

        Student[] arr = {new Student("Aslan", "A123"), s2};
        //arr[1].printInfo();

        //s2.printInfo();

        for (int i = 0; i < arr.length; i++) {
            Student s = arr[i];
            s.printInfo();
        }
        int num=(int)12.5; //explicit method --- when you restored big data to small datatype
        System.out.println(num);
        double age=10; // implicit/wide/auto when you restored small data to big  datatype

        Animal a=new Dog();
        //Dog d=new Animal(); // get error because trying restore big data to small datatype
        Dog d= (Dog)new Animal(); //explicit method

    }
} */

//s1=variable
//new = object
//Student () = calls constructor
// Student = class
// = assigning
//Polymorphism offers the following advantages − It helps the programmer to reuse the codes, i.e.,
// classes once written, tested and implemented can be reused as required. Saves a lot of time.
// Single variable can be used to store multiple data types. Easy to debug the codes.